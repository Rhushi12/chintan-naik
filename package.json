{
  "name": "chintan-naik-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site with Vercel serverless backend for contact form",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:api": "npx vercel dev",
    "dev:full": "concurrently \"vite\" \"npx vercel dev\"",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "resend": "^3.4.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "vite": "^5.4.0"
  }
}
